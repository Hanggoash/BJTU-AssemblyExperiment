# TODO：基于 ARM 汇编的 MatMul 矩阵乘法算子实现与基础测试

## 项目目标

实现一个支持任意维度的整数矩阵乘法算子：

```cpp
Matrix C = A * B;
```

其中：

```text
A: M × K
B: K × N
C: M × N
```

项目采用 C++ + AArch64 汇编混合实现：

```text
C++：负责 Matrix 类封装、维度检查、内存管理、operator*、随机矩阵生成、文件读入、测试和计时
AArch64 汇编：负责最普通的三重循环矩阵乘法计算
```

当前阶段只完成基础版，不做任何优化。

---

## 一、项目文件结构

需要实现以下文件：

```text
Matrix.h
Matrix.cpp
main.cpp
matmul_basic.s
Makefile
todo.md
```

其中：

```text
Matrix.h / Matrix.cpp：实现 Matrix 类
main.cpp：测试正确性和性能
matmul_basic.s：实现 AArch64 汇编基础版 MatMul
Makefile：编译项目
```

---

## 二、Matrix 类设计

### 1. 成员变量

Matrix 类保持简单布局，方便汇编按固定偏移读取：

```cpp
class Matrix {
public:
    int rows;
    int cols;
    int* data;
};
```

不要使用：

```text
虚函数
继承
复杂模板
std::vector 作为底层存储
```

底层必须使用一维连续数组：

```cpp
int* data;
```

二维访问通过下标转换实现：

```cpp
data[i * cols + j]
```

---

### 2. 需要实现的成员函数

```cpp
Matrix(int r, int c);
Matrix(const Matrix& other);
Matrix& operator=(const Matrix& other);
~Matrix();

int& operator()(int i, int j);
const int& operator()(int i, int j) const;

void randomFill(int low, int high);
void print() const;
bool equals(const Matrix& other) const;

static Matrix fromFile(const std::string& filename);

Matrix operator*(const Matrix& other) const;
```

说明：

```text
Matrix(int r, int c)：创建 r × c 矩阵，并初始化为 0
拷贝构造函数：必须深拷贝 data
赋值运算符：必须深拷贝 data，并处理自赋值
析构函数：释放 data
operator()：实现二维访问
randomFill：用于随机生成大矩阵
print：只用于小矩阵调试
equals：比较两个矩阵是否完全相同
fromFile：从文件读入矩阵
operator*：调用 AArch64 汇编函数完成矩阵乘法
```

暂时不需要默认构造函数。

---

## 三、Matrix 内存布局检查

在 `Matrix.h` 或 `Matrix.cpp` 中加入静态检查：

```cpp
static_assert(offsetof(Matrix, rows) == 0);
static_assert(offsetof(Matrix, cols) == 4);
static_assert(offsetof(Matrix, data) == 8);
```

需要包含头文件：

```cpp
#include <cstddef>
```

这样可以保证 AArch64 汇编中按固定偏移读取字段时不会出错。

---

## 四、随机矩阵初始化

实现：

```cpp
void Matrix::randomFill(int low, int high);
```

要求：

```text
能够把矩阵所有元素填充为 [low, high] 范围内的随机整数
随机数范围建议使用 -5 到 5 或 0 到 9
避免数值太大导致 int 溢出
```

可以先使用简单写法：

```cpp
data[i] = low + rand() % (high - low + 1);
```

后续如果需要再改成 `<random>`。

---

## 五、文件读入矩阵

实现：

```cpp
static Matrix Matrix::fromFile(const std::string& filename);
```

建议文件格式：

```text
2 3
1 2 3
4 5 6
```

第一行表示：

```text
rows cols
```

后面依次读入：

```text
rows × cols 个整数
```

示例用法：

```cpp
Matrix A = Matrix::fromFile("A.txt");
```

如果文件打不开，或者数据数量不足，可以输出错误信息并退出。

---

## 六、C++ 参考版矩阵乘法

在 `main.cpp` 或单独函数中实现 C++ 参考版：

```cpp
Matrix matmul_cpp(const Matrix& A, const Matrix& B);
```

逻辑：

```cpp
for (int i = 0; i < A.rows; i++) {
    for (int j = 0; j < B.cols; j++) {
        int sum = 0;
        for (int k = 0; k < A.cols; k++) {
            sum += A(i, k) * B(k, j);
        }
        C(i, j) = sum;
    }
}
```

作用：

```text
用于和 AArch64 汇编版结果对比
保证汇编结果正确
```

---

## 七、AArch64 汇编函数声明

在 C++ 中声明汇编函数：

```cpp
extern "C" void matmul_asm_basic(const Matrix* A, const Matrix* B, Matrix* C);
```

说明：

```text
必须使用 extern "C"
否则 C++ 会进行名字修饰，汇编中的 .global matmul_asm_basic 可能链接不上
```

---

## 八、operator* 的实现逻辑

在 `Matrix::operator*` 中完成：

```text
1. 检查维度是否合法
   当前矩阵 cols 必须等于 other.rows

2. 创建结果矩阵
   Matrix C(rows, other.cols)

3. 调用汇编函数
   matmul_asm_basic(this, &other, &C)

4. 返回 C
```

伪代码：

```cpp
Matrix Matrix::operator*(const Matrix& other) const {
    if (cols != other.rows) {
        // 输出错误并退出
    }

    Matrix C(rows, other.cols);
    matmul_asm_basic(this, &other, &C);
    return C;
}
```

---

## 九、matmul_basic.s 汇编基础版

文件名：

```text
matmul_basic.s
```

函数名：

```asm
.global matmul_asm_basic
.type matmul_asm_basic, %function
matmul_asm_basic:
```

函数参数进入汇编后的对应关系：

```text
x0 = A
x1 = B
x2 = C
```

Matrix 字段偏移：

```text
rows offset = 0
cols offset = 4
data offset = 8
```

需要读取：

```text
A.rows -> M
A.cols -> K
B.cols -> N
A.data
B.data
C.data
```

基础版逻辑必须是普通三重循环：

```cpp
for (int i = 0; i < M; i++) {
    for (int j = 0; j < N; j++) {
        int sum = 0;
        for (int k = 0; k < K; k++) {
            sum += A->data[i * K + k] * B->data[k * N + j];
        }
        C->data[i * N + j] = sum;
    }
}
```

地址计算公式：

```text
A[i][k] = A_data + (i * K + k) * 4
B[k][j] = B_data + (k * N + j) * 4
C[i][j] = C_data + (i * N + j) * 4
```

当前阶段禁止使用：

```text
MADD
指针递增优化
循环展开
分块优化
NEON SIMD
其他高级优化
```

乘加必须保持：

```asm
MUL w16, w14, w15
ADD w12, w12, w16
```

不要改成：

```asm
MADD
```

因为当前版本要作为后续优化版本的性能基准。

---

## 十、最小汇编测试建议

在正式写 `matmul_asm_basic` 前，可以先写简单汇编函数测试结构体偏移：

```text
get_rows(const Matrix* A)
get_cols(const Matrix* A)
get_first_element(const Matrix* A)
```

确认：

```text
rows 能正确读取
cols 能正确读取
data 指针能正确读取
data[0] 能正确读取
```

这些测试通过后，再写完整 MatMul 汇编。

---

## 十一、正确性测试

在 `main.cpp` 中先做小矩阵测试。

示例：

```text
A = 2 × 3
B = 3 × 2
C = 2 × 2
```

同时计算：

```cpp
Matrix C_cpp = matmul_cpp(A, B);
Matrix C_asm = A * B;
```

然后比较：

```cpp
C_cpp.equals(C_asm)
```

如果一致，输出：

```text
Correct
```

如果不一致，输出：

```text
Wrong
```

并打印两个小矩阵结果，方便排错。

---

## 十二、性能测试

正确性测试通过后，再做随机大矩阵测试。

测试规模建议：

```text
32 × 32
64 × 64
128 × 128
256 × 256
```

每组测试：

```text
1. 随机生成 A 和 B
2. 用 C++ reference 计算 C_cpp 并计时
3. 用 AArch64 ASM basic 计算 C_asm 并计时
4. 比较 C_cpp 和 C_asm 是否一致
5. 输出两个版本耗时
```

计时使用：

```cpp
std::chrono
```

输出示例：

```text
Size: 64 x 64
C++ reference time: xxx ms
ASM basic time: xxx ms
Result: Correct
```

大矩阵不要完整打印。

---

## 十三、Makefile

需要支持：

```bash
make
./matmul_test
make clean
```

编译命令大致为：

```bash
g++ -std=c++17 -Wall -Wextra -O0 main.cpp Matrix.cpp matmul_basic.s -o matmul_test
```

当前阶段建议先使用：

```text
-O0
```

原因：

```text
-O0 更方便观察普通 C++ 和手写汇编基础版的差异
```

后续可以再尝试：

```text
-O2
```

并在报告中分析编译器优化的影响。

---

## 十四、当前阶段不要做的内容

当前只实现基础版，不要提前加入优化。

不要实现：

```text
MADD 优化版
指针递增优化版
循环展开版
分块版
NEON SIMD 版
多线程版
OpenMP 版
```

这些留到后续优化计划中再做。

---

## 十五、阶段完成标准

当前阶段完成后，项目应满足：

```text
1. Matrix 类可以正常创建、访问、释放
2. Matrix 使用一维连续 int* data 存储
3. 支持随机矩阵生成
4. 支持从文件读入矩阵
5. 支持 operator* 写法
6. operator* 内部调用 AArch64 汇编基础版
7. AArch64 汇编基础版使用普通三重循环
8. 小矩阵正确性测试通过
9. 大矩阵随机测试结果正确
10. 能输出 C++ reference 和 ASM basic 的运行时间
```

---

## 十六、后续优化预留

当前基础版完成后，后续可以新增：

```text
matmul_madd.s
matmul_ptr.s
matmul_unroll.s
matmul_block.s
matmul_neon.s
```

并在 `main.cpp` 中统一测试：

```text
C++ reference
ASM basic
ASM optimized
```

但这些不是当前任务。
