# new_todo.md：MatMul 项目三种 AArch64 汇编优化版本实现与统一性能对比

## 一、当前项目目标

在已有基础版项目基础上，继续增加 3 个 AArch64 汇编优化版本，并在一次运行中统一比较以下 5 个版本的正确性和运行时间：

```text
1. C++ reference
2. ASM basic
3. ASM unroll
4. ASM addr
5. ASM neon
```

其中：

```text
ASM basic：普通三重循环基础版，作为汇编 baseline
ASM unroll：循环展开版
ASM addr：减少地址计算和访存依赖版
ASM neon：NEON SIMD 向量化版
```

要求：

```text
1. 所有版本都支持任意 M × K 和 K × N 的整数矩阵乘法
2. 所有版本都使用 Matrix 类的一维连续 int* data 存储
3. 所有版本都必须和 C++ reference 结果对比
4. 一次运行程序即可输出所有版本的耗时和正确性
5. 保留 Makefile
6. 可以额外提供 Python 脚本用于多次运行、统计平均耗时和生成结果表格
```

---

## 二、项目文件结构

建议最终文件结构如下：

```text
Matrix.h
Matrix.cpp
main.cpp

matmul_basic.s
matmul_unroll.s
matmul_addr.s
matmul_neon.s

Makefile
run_benchmark.py
new_todo.md
```

说明：

```text
Matrix.h / Matrix.cpp：Matrix 类封装
main.cpp：统一正确性测试和性能测试
matmul_basic.s：普通基础版
matmul_unroll.s：循环展开优化版
matmul_addr.s：减少地址计算优化版
matmul_neon.s：NEON SIMD 优化版
Makefile：编译项目
run_benchmark.py：可选，用于重复运行程序并统计结果
```

---

## 三、已有基础版要求保持不变

已有的 `matmul_asm_basic` 保持朴素三重循环，不要修改成优化版本。

函数声明：

```cpp
extern "C" void matmul_asm_basic(const Matrix* A, const Matrix* B, Matrix* C);
```

汇编函数名：

```asm
.global matmul_asm_basic
.type matmul_asm_basic, %function
matmul_asm_basic:
```

基础版要求：

```text
1. 使用普通三重循环
2. 地址计算保持直观
3. 乘加使用 MUL + ADD
4. 不使用 MADD
5. 不使用循环展开
6. 不使用 NEON
7. 作为后续优化版本的对比基准
```

---

## 四、新增优化版本 1：循环展开版 matmul_asm_unroll

### 1. 函数声明

在 C++ 中新增：

```cpp
extern "C" void matmul_asm_unroll(const Matrix* A, const Matrix* B, Matrix* C);
```

### 2. 汇编文件

新建：

```text
matmul_unroll.s
```

函数名：

```asm
.global matmul_asm_unroll
.type matmul_asm_unroll, %function
matmul_asm_unroll:
```

### 3. 优化思想

该版本对应循环展开优化。

基础版内层循环是：

```cpp
for (int k = 0; k < K; k++) {
    sum += A[i][k] * B[k][j];
}
```

优化版建议先做 4 路展开：

```cpp
for (int k = 0; k + 3 < K; k += 4) {
    sum += A[i][k]     * B[k][j];
    sum += A[i][k + 1] * B[k + 1][j];
    sum += A[i][k + 2] * B[k + 2][j];
    sum += A[i][k + 3] * B[k + 3][j];
}

for (; k < K; k++) {
    sum += A[i][k] * B[k][j];
}
```

### 4. 实现要求

```text
1. 外层 i、j 循环仍然保持普通写法
2. 只展开最内层 k 循环
3. 先处理 k + 3 < K 的部分
4. 剩余不足 4 个的 k 用普通循环处理
5. 乘加仍然可以使用 MUL + ADD
6. 暂时不要和 MADD 混在一起，避免优化来源不清楚
```

### 5. 目的

该版本主要观察：

```text
1. 减少内层循环的 cmp 和 b 跳转次数
2. 增加连续计算指令数量
3. 提高流水线利用率
```

---

## 五、新增优化版本 2：减少地址计算版 matmul_asm_addr

### 1. 函数声明

在 C++ 中新增：

```cpp
extern "C" void matmul_asm_addr(const Matrix* A, const Matrix* B, Matrix* C);
```

### 2. 汇编文件

新建：

```text
matmul_addr.s
```

函数名：

```asm
.global matmul_asm_addr
.type matmul_asm_addr, %function
matmul_asm_addr:
```

### 3. 优化思想

基础版在内层循环中反复计算：

```text
A_data + (i * K + k) * 4
B_data + (k * N + j) * 4
C_data + (i * N + j) * 4
```

优化版应尽量减少重复地址计算。

可以改成类似下面的 C++ 思路：

```cpp
for (int i = 0; i < M; i++) {
    int* Arow = A_data + i * K;
    int* Crow = C_data + i * N;

    for (int j = 0; j < N; j++) {
        int sum = 0;

        for (int k = 0; k < K; k++) {
            sum += Arow[k] * B_data[k * N + j];
        }

        Crow[j] = sum;
    }
}
```

### 4. 实现要求

```text
1. 在 i 循环中提前计算 A 当前行首地址 Arow
2. 在 i 循环中提前计算 C 当前行首地址 Crow
3. 在 j 循环中 C[i][j] 存储时直接使用 Crow + j * 4
4. 内层 k 循环中 A[i][k] 直接用 Arow + k * 4
5. B[k][j] 仍然按 B_data + (k * N + j) * 4 访问
6. 尽量减少内层循环里的 MUL 指令数量
```

### 5. 可选进一步改进

如果实现顺利，可以把 B 的访问也改成指针递增形式：

```text
进入 k 循环前：
Bptr = B_data + j * 4

每次 k++：
Bptr += N * 4
```

这样内层循环中访问 B 时可以用：

```text
ldr w?, [Bptr]
add Bptr, Bptr, N * 4
```

但注意：

```text
如果 N * 4 超过立即数范围，需要提前用寄存器保存 stride
```

### 6. 目的

该版本主要观察：

```text
1. 减少重复乘法地址计算
2. 减少循环内部指令数量
3. 降低访存地址计算依赖
4. 提高普通整数汇编版本性能
```

---

## 六、新增优化版本 3：NEON SIMD 版 matmul_asm_neon

### 1. 函数声明

在 C++ 中新增：

```cpp
extern "C" void matmul_asm_neon(const Matrix* A, const Matrix* BT, Matrix* C);
```

注意：这里第二个参数建议传入 `BT`，也就是 B 的转置矩阵。

原矩阵乘法是：

```text
A:  M × K
B:  K × N
C:  M × N
```

转置后：

```text
BT: N × K
```

那么：

```text
C[i][j] = dot(A 的第 i 行, BT 的第 j 行)
```

这样 A 的一行和 BT 的一行都是连续内存，更适合 NEON 加速。

### 2. 为什么 NEON 版建议使用 BT

如果直接用 B：

```text
B[k][j]
```

在行优先存储下是跨行访问，不连续。

如果使用 BT：

```text
BT[j][k]
```

就变成连续访问。

这样内层点积可以变成：

```cpp
for (int k = 0; k < K; k++) {
    sum += A[i][k] * BT[j][k];
}
```

A 和 BT 都是连续访问，更适合一次加载多个 int。

### 3. 汇编文件

新建：

```text
matmul_neon.s
```

函数名：

```asm
.global matmul_asm_neon
.type matmul_asm_neon, %function
matmul_asm_neon:
```

### 4. NEON 版基本思路

使用 128 位向量寄存器，一次处理 4 个 int。

伪代码：

```cpp
for (int i = 0; i < M; i++) {
    for (int j = 0; j < N; j++) {
        vector_sum = 0;

        for (int k = 0; k + 3 < K; k += 4) {
            load 4 int from A[i][k ... k+3]
            load 4 int from BT[j][k ... k+3]
            vector_sum += vector_A * vector_BT
        }

        sum = horizontal_add(vector_sum)

        for (; k < K; k++) {
            sum += A[i][k] * BT[j][k]
        }

        C[i][j] = sum
    }
}
```

### 5. 推荐使用的 NEON 指令

可以考虑使用：

```asm
ld1 {v0.4s}, [xA]
ld1 {v1.4s}, [xB]
mul v2.4s, v0.4s, v1.4s
add vSum.4s, vSum.4s, v2.4s
addv sTemp, vSum.4s
fmov wTemp, sTemp
```

说明：

```text
v0.4s：4 个 32 位整数
v1.4s：4 个 32 位整数
mul v2.4s：并行做 4 个 int 乘法
add vSum.4s：累加向量结果
addv：把向量内部 4 个 lane 加成一个标量
```

### 6. NEON 版实现要求

```text
1. C++ 层先生成 B 的转置矩阵 BT
2. matmul_asm_neon 的第二个参数传 BT，不直接传 B
3. NEON 主循环每次处理 4 个 int
4. K 不是 4 的倍数时，剩余部分用普通标量循环处理
5. 结果必须和 C++ reference 完全一致
6. 先只支持 int32，不做 int64
```

### 7. C++ 层需要新增 transpose 函数

在 Matrix 类中新增：

```cpp
Matrix transpose() const;
```

实现逻辑：

```cpp
Matrix T(cols, rows);

for (int i = 0; i < rows; i++) {
    for (int j = 0; j < cols; j++) {
        T(j, i) = (*this)(i, j);
    }
}

return T;
```

使用方式：

```cpp
Matrix BT = B.transpose();
matmul_asm_neon(&A, &BT, &C_neon);
```

### 8. NEON 版计时注意事项

为了公平，建议分两种时间：

```text
1. neon kernel time：只统计 matmul_asm_neon 本身耗时
2. neon total time：统计 B.transpose() + matmul_asm_neon 总耗时
```

输出时可以同时显示：

```text
ASM neon kernel time: xxx ms
ASM neon total time:  xxx ms
```

这样报告中可以分析：

```text
NEON 计算核心可能更快，但转置 B 本身也有额外开销。
```

---

## 七、C++ 层统一接口设计

### 1. 汇编函数声明

在合适的位置加入：

```cpp
extern "C" void matmul_asm_basic(const Matrix* A, const Matrix* B, Matrix* C);
extern "C" void matmul_asm_unroll(const Matrix* A, const Matrix* B, Matrix* C);
extern "C" void matmul_asm_addr(const Matrix* A, const Matrix* B, Matrix* C);
extern "C" void matmul_asm_neon(const Matrix* A, const Matrix* BT, Matrix* C);
```

### 2. Matrix 类中可以保留 operator*

`operator*` 仍然调用 basic 版：

```cpp
Matrix Matrix::operator*(const Matrix& other) const {
    if (cols != other.rows) {
        // error
    }

    Matrix C(rows, other.cols);
    matmul_asm_basic(this, &other, &C);
    return C;
}
```

说明：

```text
operator* 保持调用 basic 版即可。
统一性能测试时，不要只依赖 operator*，而是直接分别调用各个汇编函数。
```

### 3. 新增 benchmark 专用函数

建议在 `main.cpp` 中写几个辅助函数：

```cpp
Matrix matmul_cpp(const Matrix& A, const Matrix& B);

double time_cpp(const Matrix& A, const Matrix& B, Matrix& C);
double time_asm_basic(const Matrix& A, const Matrix& B, Matrix& C);
double time_asm_unroll(const Matrix& A, const Matrix& B, Matrix& C);
double time_asm_addr(const Matrix& A, const Matrix& B, Matrix& C);
double time_asm_neon(const Matrix& A, const Matrix& BT, Matrix& C);
```

也可以不拆这么细，但主函数中必须能清楚区分每个版本。

---

## 八、一次运行中的正确性和性能测试流程

`main.cpp` 的主流程建议如下：

```text
1. 设置随机种子
2. 定义测试规模列表
3. 对每个规模执行测试
4. 随机生成 A 和 B
5. C++ reference 计算 C_cpp，并计时
6. ASM basic 计算 C_basic，并计时
7. ASM unroll 计算 C_unroll，并计时
8. ASM addr 计算 C_addr，并计时
9. 生成 BT = B.transpose()
10. ASM neon 计算 C_neon，并计时
11. 比较每个汇编版本是否等于 C_cpp
12. 输出当前规模下所有版本结果
```

测试规模建议：

```text
32 × 32
64 × 64
128 × 128
256 × 256
```

如果运行时间太长，可以先用：

```text
16 × 16
32 × 32
64 × 64
128 × 128
```

矩阵维度也可以测试非方阵：

```text
64 × 128 乘 128 × 64
128 × 64 乘 64 × 128
```

---

## 九、输出格式要求

建议程序输出类似：

```text
==================================================
Size: 64 x 64
A: 64 x 64, B: 64 x 64

C++ reference:      12.345 ms
ASM basic:          18.234 ms    Correct
ASM unroll:         15.678 ms    Correct
ASM addr:           11.456 ms    Correct
ASM neon kernel:     6.789 ms    Correct
ASM neon total:      7.321 ms    Correct

Speedup vs basic:
unroll: 1.16x
addr:   1.59x
neon:   2.69x
==================================================
```

说明：

```text
Speedup vs basic = ASM basic time / 当前版本 time
```

如果某个版本结果错误，输出：

```text
Wrong
```

并且在小矩阵测试时打印 C++ 和错误版本的结果，方便排查。

---

## 十、Python 脚本 run_benchmark.py

可以额外写一个 Python 脚本，用来多次运行程序，保存输出结果。

### 1. 脚本目标

```text
1. 执行 make clean && make
2. 多次运行 ./matmul_test
3. 收集输出
4. 保存到 benchmark_result.txt
```

### 2. 简单版本功能

`run_benchmark.py` 可以先只做：

```python
import subprocess
from datetime import datetime

def run_cmd(cmd):
    return subprocess.run(cmd, shell=True, text=True, capture_output=True)

with open("benchmark_result.txt", "w") as f:
    f.write("MatMul Benchmark Result\n")
    f.write(str(datetime.now()) + "\n\n")

    build = run_cmd("make clean && make")
    f.write("===== Build Output =====\n")
    f.write(build.stdout)
    f.write(build.stderr)
    f.write("\n")

    for i in range(3):
        f.write(f"===== Run {i + 1} =====\n")
        result = run_cmd("./matmul_test")
        f.write(result.stdout)
        f.write(result.stderr)
        f.write("\n")
```

### 3. 可选增强

后续可以继续增强脚本：

```text
1. 解析每个版本的 ms 时间
2. 计算多次运行平均值
3. 输出 CSV 文件
4. 用 pandas 或 matplotlib 画图
```

当前阶段先不强制。

---

## 十一、Makefile 修改要求

Makefile 保留，并加入新的汇编文件。

编译目标中需要包含：

```text
main.cpp
Matrix.cpp
matmul_basic.s
matmul_unroll.s
matmul_addr.s
matmul_neon.s
```

示例编译命令：

```bash
g++ -std=c++17 -Wall -Wextra -O0 main.cpp Matrix.cpp matmul_basic.s matmul_unroll.s matmul_addr.s matmul_neon.s -o matmul_test
```

Makefile 至少支持：

```bash
make
make clean
```

可选支持：

```bash
make run
make bench
```

其中：

```bash
make run
```

执行：

```bash
./matmul_test
```

```bash
make bench
```

执行：

```bash
python3 run_benchmark.py
```

---

## 十二、编译优化等级要求

当前建议保留：

```text
-O0
```

原因：

```text
1. 更容易观察手写汇编和普通 C++ 的差异
2. 更容易调试
3. 更适合作为实验初期结果
```

后续可以增加：

```text
-O2
```

并对比：

```text
-O0 下 C++ reference 和汇编版本的差异
-O2 下 C++ reference 可能被编译器优化得很强
```

不要把 `-O2` 下 C++ 比汇编快理解成失败，可以在报告中分析现代编译器优化能力。

---

## 十三、正确性测试建议

在跑大矩阵前，必须先跑小矩阵。

小矩阵建议：

```text
A = 2 × 3
B = 3 × 2
```

例如：

```text
A:
1 2 3
4 5 6

B:
7  8
9  10
11 12
```

正确结果：

```text
58  64
139 154
```

所有版本必须得到相同结果。

还需要测试：

```text
1 × 1
1 × N 乘 N × 1
M × 1 乘 1 × N
非方阵
K 不是 4 的倍数
```

尤其是 `unroll` 和 `neon` 版本，必须测试 `K` 不是 4 的倍数的情况，例如：

```text
3
5
7
9
```

否则尾部处理可能出错。

---

## 十四、性能测试建议

建议随机矩阵元素范围：

```text
-5 到 5
```

或者：

```text
0 到 9
```

避免数值太大导致 `int` 溢出。

建议测试规模：

```text
32 × 32
64 × 64
128 × 128
256 × 256
```

如果 256 × 256 太慢，可以先改成：

```text
16 × 16
32 × 32
64 × 64
128 × 128
```

大矩阵不要打印完整结果，只输出：

```text
规模
各版本耗时
正确性
相对 basic 的加速比
```

---

## 十五、每个版本在报告中的解释重点

### 1. ASM basic

解释重点：

```text
普通三重循环
地址计算直观
没有优化
作为 baseline
```

### 2. ASM unroll

解释重点：

```text
对 k 循环进行循环展开
减少循环判断和跳转次数
增加连续计算指令
提高流水线利用率
```

### 3. ASM addr

解释重点：

```text
提前计算 A 当前行首地址和 C 当前行首地址
减少内层循环中的重复乘法和地址计算
降低访存地址计算依赖
```

### 4. ASM neon

解释重点：

```text
使用 NEON 128 位向量寄存器
一次处理 4 个 int32 元素
通过转置 B 让点积两侧都连续访问
提升数据并行度
```

---

## 十六、实现顺序建议

不要同时写三个优化版本，建议按下面顺序实现：

```text
第 1 步：保持 basic 版不变，确认当前项目能正常编译运行

第 2 步：修改 main.cpp，让它支持统一测试 C++ reference 和 ASM basic

第 3 步：新增 matmul_unroll.s
先只测试小矩阵，确认正确后加入性能测试

第 4 步：新增 matmul_addr.s
先只测试小矩阵，确认正确后加入性能测试

第 5 步：给 Matrix 增加 transpose()
先在 C++ 中测试转置是否正确

第 6 步：新增 matmul_neon.s
先测试 K 是 4 的倍数的情况
再测试 K 不是 4 的倍数的情况

第 7 步：完善 main.cpp 输出格式
一次运行输出所有版本对比结果

第 8 步：完善 Makefile
加入所有 .s 文件

第 9 步：可选新增 run_benchmark.py
多次运行并保存结果
```

---

## 十七、阶段完成标准

完成后，项目应满足：

```text
1. make 可以成功编译
2. ./matmul_test 可以一次运行所有测试
3. 同时输出 C++、basic、unroll、addr、neon 的耗时
4. 所有汇编版本结果都和 C++ reference 一致
5. 能输出相对 basic 的加速比
6. K 不是 4 的倍数时 unroll 和 neon 也正确
7. run_benchmark.py 可以重复运行并保存结果
8. basic 版保持朴素，不混入优化
9. 三个优化版本分别体现不同优化思想
10. 结果可以直接用于实验报告中的性能对比分析
```

---

## 十八、当前不要做的内容

当前不要加入：

```text
多线程
OpenMP
GPU
浮点矩阵乘法
模板矩阵类
复杂异常处理
自动画图
复杂命令行参数解析
```

当前重点是：

```text
AArch64 汇编基础版 + 三个优化版 + 正确性验证 + 统一性能对比
```
